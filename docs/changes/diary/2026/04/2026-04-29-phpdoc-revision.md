# Дневник — 2026-04-29 (phpdoc-revision)

## 14:00 (Europe/Kyiv) docs[convention.phpdoc] — Ревизия PHPDoc по `app/` и правило в SKILLS.md
**Entry ID:** 01JPHPDOC20260429
**Agent:** Composer
**Дата:** 2026-04-29
**Ветка:** (локально, не задана)

### Файлы
- `app/**/*.php` — добавлены класс- и метод-уровень PHPDoc (описания, `@param`/`@return` где уместно, усилены `@mixin` у API Resources)
- `.cursor/skills/SKILLS.md` — секция **PHPDoc (source documentation)**
- `.cursor/skills/use-cursor-skills-folder/SKILL.md` — отсылка к PHPDoc в таблице

### Что сделано
Проведена ревизия исходников в `app/`: у классов появились краткие англоязычные описания назначения; у сервисов, форм-реквестов, контроллеров, middleware, провайдеров, перечислений и ресурсов уточнены PHPDoc-блоки там, где нужен контракт (в т. ч. array shapes и связи с доменными классами через `@see`).
В репозиторий зафиксировано правило документирования в `.cursor/skills/SKILLS.md`, чтобы последующие изменения следовали тем же соглашениям.

### Почему
Единообразная документация для IDE/статического анализа и онбординга; явное разделение «класс описываем целиком, метод — при нетривиальной семантике».

### Влияние
- **БД:** N/A
- **API:** N/A (только комментарии)
- **Производительность:** N/A

### Проверено
- Тесты: N/A (логика не менялась)
- Линтер: диагностика IDE по затронутым файлам — без ошибок

### Follow-up
- [ ] При необходимости: `./vendor/bin/sail artisan test` пользователем
- [ ] `vendor/bin/pint --dirty` пользователем при желании выровнять стиль

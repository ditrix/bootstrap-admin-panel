# Дневник изменений — 2026-03-30 (main)

## 19:54 (Europe/Riga) feat[views.admin,frontend.vite,styles.admin.theme] — SCSS-пайплайн для админских стилей
**Entry ID:** 01J0SCSS20260330
**Дата:** 2026-03-30
**Ветка:** main

### Файлы
- `vite.config.js` (+4 −1)
- `resources/views/admin/partials/head.blade.php` (+1 −0)
- `resources/themes/admin/assets/css/*` (новая структура SCSS)
- `package.json`, `package-lock.json` (devDependency `sass`)

### Что сделано
Добавлен новый SCSS entrypoint `resources/themes/admin/assets/css/app.scss` и набор блоков в `blocks/` для компонентной структуры стилей. Подключен Vite-ресурс в `admin/partials/head.blade.php` и добавлен вход в `vite.config.js`. Для компиляции SCSS установлен `sass`.

### Почему
Нужен переход от полностью статического CSS к управляемой SCSS-сборке с расширяемой архитектурой блоков.

### Влияние
- **БД:** N/A
- **API:** N/A
- **Производительность:** возможна оптимизация размера и кэширования через Vite-сборку

### Проверено
- Тесты: N/A
- Линтер: ok (`sail bin pint --dirty --format agent`)

### Follow-up
- [ ] Перенести правила из `public/maket/css/styles.css` в SCSS-блоки поэтапно

## 20:14 (Europe/Riga) feat[views.admin,frontend.vite,styles.admin.theme] — Перенос SB Admin стилей по SCSS-блокам
**Entry ID:** 01J0SCSSBLOCKS20260330
**Дата:** 2026-03-30
**Ветка:** main

### Файлы
- `resources/themes/admin/assets/css/app.scss` (+6 −1)
- `resources/themes/admin/assets/css/_base.scss` (+5 −0)
- `resources/themes/admin/assets/css/blocks/_layout.scss` (+83 −0)
- `resources/themes/admin/assets/css/blocks/_auth.scss` (+13 −0)
- `resources/themes/admin/assets/css/blocks/_error.scss` (+17 −0)
- `resources/themes/admin/assets/css/blocks/_nav.scss` (+142 −4)
- `resources/themes/admin/assets/css/blocks/_subnav.scss` (+38 −1)
- `resources/themes/admin/assets/css/blocks/_datatable.scss` (+162 −0)
- `resources/views/admin/partials/head.blade.php` (+0 −1)
- `resources/views/admin/layouts/auth.blade.php` (+1 −1)
- `resources/views/admin/layouts/error.blade.php` (+1 −1)
- `package.json`, `package-lock.json` (dependency `bootstrap`)

### Что сделано
Удалено использование `public/maket/css/styles.css` в админских Blade-layout. В `app.scss` подключен Bootstrap из `node_modules` и перенесены кастомные стили SB Admin в тематические блоки (`layout`, `auth`, `error`, `nav`, `subnav`, `datatable`). Структура блоков выровнена под реальные Blade-шаблоны админки.

### Почему
Требовалось полностью отвязать админку от статического `styles.css` и перейти на поддерживаемую SCSS-архитектуру по блокам.

### Влияние
- **БД:** N/A
- **API:** N/A
- **Производительность:** CSS собирается через Vite и кэшируется как build-asset

### Проверено
- Тесты: N/A
- Линтер: ok (`sail bin pint --dirty --format agent`)

### Follow-up
- [ ] По мере развития UI добавлять новые блоки вместо расширения существующих крупных файлов

## 20:38 (Europe/Riga) fix[styles.admin.theme] — Возврат fixed-layout для topnav и sidenav
**Entry ID:** 01J0FIXEDLAYOUT20260330
**Дата:** 2026-03-30
**Ветка:** main

### Файлы
- `resources/themes/admin/assets/css/blocks/_layout.scss` (+8 −5)
- `resources/themes/admin/assets/css/blocks/_nav.scss` (+0 −1)

### Что сделано
Восстановлено фиксированное поведение layout по паттерну SB Admin: верхний `sb-topnav` и левый `layoutSidenav_nav` снова закреплены и не прокручиваются вместе с контентом. Возвращены смещения контентной области (`padding-left` и `top`) для корректной прокрутки только основного содержимого.

### Почему
После промежуточных правок панель начала скроллиться вместе со страницей и перестала соответствовать ожидаемому UX.

### Влияние
- **БД:** N/A
- **API:** N/A
- **Производительность:** N/A

### Проверено
- Тесты: N/A
- Линтер: ok

### Follow-up
- [ ] При следующих изменениях layout проверять fixed/scroll поведение на длинных страницах dashboard и tables
